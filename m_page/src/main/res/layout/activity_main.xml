<com.flipboard.bottomsheet.BottomSheetLayout
  android:id="@+id/bottomsheet"
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  xmlns:app="http://schemas.android.com/apk/res-auto">
<!-- 1st commit done -->
  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
      android:background="@drawable/mappic">

   <android.support.v7.widget.Toolbar
      android:id="@+id/main_toolbar"
      android:layout_width="match_parent"
      android:layout_height="?attr/actionBarSize"
      android:background="?attr/colorPrimary"
      app:titleTextColor="#fff"
       android:visibility="invisible"/>

    <Button
      android:id="@+id/bottomsheet_fragment_button"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_gravity="center_horizontal"
      android:layout_marginBottom="16dp"
      android:text="method 1 (using AppBarLayout, CoordinatorLayout,...)"
        android:visibility="invisible"/>

    <Button
      android:id="@+id/bottomsheet_fragment_button2"
      android:layout_width="60dp"
      android:layout_height="wrap_content"
      android:layout_gravity="center_horizontal"
      android:layout_marginBottom="16dp"
      android:text="Cick here to go to maptag page"
        android:background="#a833ff"/>

      <Button
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:id="@+id/goto_invite_page"
          android:background="#a833ff"
          android:text="Go to invite page"
          />


    <EditText
      android:id="@+id/bottomsheet_fragment_editText"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:focusable="true"
      android:focusableInTouchMode="true"
      android:hint="try me while bottom sheet appears"
      android:imeOptions="actionSearch|flagNoExtractUi"
      android:singleLine="true"
        android:visibility="invisible"/>

    <View
      android:id="@+id/focusStealer"
      android:layout_width="0px"
      android:layout_height="0px"
      android:focusable="true"
      android:focusableInTouchMode="true"/>

  </LinearLayout>
</com.flipboard.bottomsheet.BottomSheetLayout>
